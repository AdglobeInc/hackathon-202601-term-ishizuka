# batch

セットアップ手順：

```bash
# batch ディレクトリに移動
cd batch

# 仮想環境の作成
python3 -m venv .venv

# ライブラリのインストール
./.venv/bin/pip3 install -r requirements.txt


実行方法（仮想環境を有効化する場合）：

```bash
source .venv/bin/activate
python3 main.py
# 終了時
deactivate
```
